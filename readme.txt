DB init:



CREATE TABLE users (
  id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) NOT NULL UNIQUE,
  password VARCHAR(128) NOT NULL,
  salt2 VARCHAR(4) NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
